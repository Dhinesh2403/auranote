name: Deploy Web (gh-pages branch)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: gh-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.MY_PAT_TOKEN }}
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build -- --configuration=production --base-href "/${{ github.event.repository.name }}/"

      - name: Inject Environment Variables
        if: always()
        run: |
          # Verify build output path from angular.json is 'www' and write env.js directly into it
          mkdir -p ./www/assets
          echo "window.__ENV = { AURA_ENDPOINT: '${{ secrets.AURA_ENDPOINT }}', AURA_API_KEY: '${{ secrets.AURA_API_KEY }}' };" > ./www/assets/env.js

      - name: Configure git user
        run: |
          git config --global user.name "Dhinesh2403"
          git config --global user.email "Dhinesh2403@users.noreply.github.com"

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.MY_PAT_TOKEN }}
          publish_dir: ./www
          publish_branch: gh-pages
          force_orphan: true
          user_name: Dhinesh2403
          user_email: Dhinesh2403@users.noreply.github.com
