name: Deploy Web (gh-pages branch)

on:
  push:
    branches:
      - demo
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gh-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build (GitHub Pages)
        run: npm run build -- --configuration=production --base-href "/${{ github.event.repository.name }}/"

      - name: Inject runtime env (GROQ_API_KEY)
        shell: bash
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          if [ -z "$GROQ_API_KEY" ]; then
            echo "Missing GROQ_API_KEY secret" >&2
            exit 1
          fi
          cat > www/assets/env.js <<'EOF'
          (function () {
            window.__ENV = window.__ENV || {};
            window.__ENV.GROQ_API_KEY = "__GROQ_API_KEY__";
          })();
          EOF
          # Replace placeholder safely
          sed -i "s/__GROQ_API_KEY__/${GROQ_API_KEY//\//\\/}/g" www/assets/env.js

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./www
          publish_branch: gh-pages
          force_orphan: true
